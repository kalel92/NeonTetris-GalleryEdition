name: Build Executables

on:
  push:
    branches: [ "desktop-version" ]
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller numpy pygame-ce

    - name: Build with PyInstaller (Windows)
      if: matrix.os == 'windows-latest'
      run: pyinstaller --onefile --windowed --name "NeonTetrisPro_Win" main.py

    - name: Build with PyInstaller (Linux)
      if: matrix.os == 'ubuntu-latest'
      run: pyinstaller --onefile --windowed --name "NeonTetrisPro_Linux" main.py

    - name: Upload Artifacts
      uses: actions/upload-artifact@v3
      with:
        name: NeonTetrisPro-${{ matrix.os }}
        path: dist/NeonTetrisPro_*
